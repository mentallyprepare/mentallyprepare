[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
command = "npm install"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/health"
healthcheckTimeout = 60

[env]
NODE_ENV = "production"
DATA_DIR = "/data/db"
