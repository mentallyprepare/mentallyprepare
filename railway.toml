[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
command = "npm install"

[deploy]
startCommand = "node server.js"
healthcheckPath = "/api/ready"
healthcheckTimeout = 60

[env]
NODE_ENV = "production"
DATA_DIR = "/data/db"
