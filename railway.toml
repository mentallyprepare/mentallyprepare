# Railway deployment configuration for Node.js
[build]
  command = "npm install"
[start]
  command = "node server.js"

# Runtime defaults for Railway
[env]
  NODE_ENV = "production"
  DATA_DIR = "/data/db"
